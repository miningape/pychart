let stuff = [1, 2, 3];

func $map(array, fn) {
  let pos = 0;
  let rtn = [];

  while (pos < $len(array)) {
    $push( rtn, fn(array[pos]) );
    pos = pos + 1;
  }

  rtn;
}

func square(num) { num * num; }

print($map(stuff, square));




